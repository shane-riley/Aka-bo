# We need to develop frontend javascript
# Two inputs from the client
  # One being the cellNumber where we are working at
  # The other being a string of the current state of the board
# Methods
  # One for all the win conditions
  # One to transfer the current state string to the 2-d array to work on
  # 